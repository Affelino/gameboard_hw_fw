## Tools

Includes a simple TCP terminal (`gameboard_test_terminal.c`) to test board communication using the Gameboard UI Protocol.

To build:
```bash
gcc gameboard_test_terminal.c -o gameboard_test_terminal
